@page
@model EersteWebsite.Pages.IndexModel
@{
}

<form method="post">
    <ul>
        @for(int i = 0; i < Model.Videogames.Count;i++)
        {
            <li><input asp-for="@Model.Videogames[i].Id" type="hidden"/> <input asp-for="@Model.Videogames[i].Titel" /><a asp-page="/Detail" asp-route-id="@Model.Videogames[i].Id">Toon meer</a></li>
        }
    </ul>
    <input type="submit" value="Opslaan"/>
</form>

